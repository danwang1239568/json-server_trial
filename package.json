{
  "name": "api-test2",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
	"serve": "json-server ./public/db.json",
	"run-all": "pnpm build && pnpm serve"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  },
  "dependencies": {
    "json-server": "1.0.0-beta.3"
  }
}
